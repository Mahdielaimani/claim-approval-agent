# Training, tuning and exploration. Not installed in the serving image.
# Install:  pip install -r requirements.txt -r requirements-train.txt

-r requirements.txt

# ---- Source data ----
openpyxl==3.1.5              # the extract is .xlsx; the API never reads it

# ---- Candidate estimators ----
# Six baselines compete; only the winner's library is needed to serve. Keeping all four
# here rather than in requirements.txt is what removed 1.2 GB from the image.
xgboost==2.1.3
catboost==1.2.7
lightgbm==4.5.0
imbalanced-learn==0.12.4     # SMOTE, inside the MLP pipeline only

# ---- Tuning ----
optuna==4.1.0

# ---- Visualisation ----
# matplotlib arrives with shap, but pinned here because the notebooks depend on the version.
matplotlib==3.9.3
seaborn==0.13.2
plotly==5.24.1
