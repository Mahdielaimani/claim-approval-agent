# Training, tuning and exploration. Kept out of the serving image, which removed 1.2 GB.
# Install:  pip install -r requirements-train.txt

-r requirements.txt

openpyxl==3.1.5              # the extract is .xlsx; the API never reads it

xgboost==2.1.3
catboost==1.2.7
lightgbm==4.5.0
imbalanced-learn==0.12.4     # SMOTE, inside the MLP pipeline only
optuna==4.1.0

matplotlib==3.9.3
seaborn==0.13.2
plotly==5.24.1
