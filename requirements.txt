# Serving dependencies only. Python 3.12.
# Training and plotting live in requirements-train.txt; test tooling in requirements-dev.txt.

numpy==1.26.4
pandas==2.2.3
scipy==1.14.1

# Random Forest is the champion, so no other estimator library is needed to serve.
# Promoting a boosted-tree model means adding its library back here.
scikit-learn==1.5.2

shap==0.46.0                 # pulls numba, llvmlite and matplotlib transitively
mlflow==2.19.0               # registry lookup at startup, with a local artefact fallback

litellm==1.55.9
jinja2==3.1.4
langfuse==2.57.0

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.14.2

python-dotenv==1.2.2
pyyaml==6.0.2
joblib==1.4.2
