# Development, test, and evaluation tooling.
# Install:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# ---- Testing ----
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx>=0.27,<0.29           
                           

# ---- LLM evaluation ----
# 4.x, not the 2.x line: deepeval 2.1.4 imports langchain.schema, which LangChain removed
# in 1.0, so it fails at import and takes the whole pytest session with it. 4.x dropped the
# LangChain dependency entirely — which also keeps a framework DESIGN.md explicitly
# rejects out of the environment.
deepeval==4.1.4              # faithfulness, hallucination, persona adherence
# deepeval pins click<8.4 while huggingface-hub asks for >=8.4.2. The constraints are
# mutually exclusive and `pip check` will say so. 8.3.3 is chosen because click is on
# deepeval's import path and only on huggingface-hub's CLI path, which nothing here uses.
click==8.3.3

# ---- Lint / types ----
ruff==0.8.4
mypy==1.14.0
types-PyYAML==6.0.12.20260724   # settings.py reads every config through yaml.safe_load

# ---- Notebooks ----
jupyter==1.1.1
ipykernel==6.29.5

