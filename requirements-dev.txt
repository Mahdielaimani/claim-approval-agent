# Everything, plus test and evaluation tooling. This is what CI installs.
# Install:  pip install -r requirements-dev.txt

-r requirements-train.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx>=0.27,<0.29

# Do not downgrade to the 2.x line: it imports langchain.schema, removed in LangChain 1.0,
# and its pytest plugin auto-loads, so the whole session fails at collection.
deepeval==4.1.4

# `pip check` will flag this: deepeval wants click<8.4, huggingface-hub wants >=8.4.2.
# 8.3.3 satisfies deepeval, and huggingface-hub only needs click for a CLI nothing here uses.
click==8.3.3

ruff==0.8.4
mypy==1.14.0
types-PyYAML==6.0.12.20260724

jupyter==1.1.1
ipykernel==6.29.5
